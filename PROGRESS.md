# JATTEA LP 制作 進捗記録

最終更新: 2026-08-14

---

## 1. 依頼内容

`~/Desktop/Frame 39.pdf`（1440 × 19195px の縦長LPデザイン、1ページPDF）をもとに、
**画像を丸ごと貼り付けるのではなく、すべてをHTML/CSSの要素として組み直した**ランディングページを作る。

条件:

- 文章・色味・レイアウトが整っていること
- リンクが実際に機能すること
- 画像が必要な箇所は、あとから差し替えられる形になっていること
- **Cloudflare で作る**（後から追加指示）

判断が必要だった点は以下の通り確認済み:

| 論点 | 決定 |
|---|---|
| CTAのリンク先 | URLは後で差し替える（1箇所で管理する構成のまま納品） |
| デプロイ範囲 | 本番デプロイまで実行する |
| PDFの表記ゆれ | PDF通りに忠実に再現する |

---

## 2. 成果物

- **公開URL: https://jattea-lp.pages.dev/**
- ソース: `~/Desktop/jattea-lp/`
- Cloudflare Pages プロジェクト名: `jattea-lp`（アカウント: kazika48@gmail.com）

```
~/Desktop/jattea-lp/
├── README.md          差し替え手順・運用メモ
├── PROGRESS.md        このファイル
├── package.json       npm run dev / npm run deploy
├── wrangler.toml      pages_build_output_dir = "public"
└── public/
    ├── index.html     全セクションの本文・リンク（451行）
    ├── robots.txt
    ├── _headers       キャッシュ／セキュリティヘッダ
    └── assets/
        ├── css/style.css   デザイントークン（:root）で色・幅を一元管理
        └── img/            写真・ロゴ・装飾 計38点（約3.5MB）
```

---

## 3. 作業ログ

### 3-1. PDFの解析

- PDF内テキストはCIDフォント埋め込みで、`get_text()` では文字化けして読めなかった
  （`FC95ŠU‰n umÁüàLÕłQ‰` のような出力になる）
- そのため **ページを13分割して画像レンダリングし、目視で全文を書き起こし**た
- デザイン上のテキストの多くは「透明PNG画像として配置された文字」だったため、
  抽出画像のうち黒背景サムネイルはすべて文字画像 → 使わずにHTMLで打ち直した

### 3-2. 写真の抽出

- ページ内の埋め込み画像を `xref` 単位で列挙し、bbox（配置座標）からどのセクションの
  どのスロットかを特定
- 全画像に **SMask（アルファマスク）** が付いていたため、`extract_image` の本体と
  smaskを合成 → `getbbox()` でトリミング → 最大1400px にリサイズして書き出し
  （マスクを無視すると背景が真っ黒になる）
- 透過が実質不要なものはJPEG、必要なものはPNGで保存
- ロゴ・花の装飾はベクター描画だったので、ページの該当領域を高解像度レンダリングして切り出し

### 3-3. 実装

- セクション構成（PDF準拠）:
  ヘッダー（sticky）→ HERO → CTA① → ISSUE（中国茶ブーム／市場データ）→ FC 0次募集 →
  MISSION → CTA② → 選ばれる4つの理由 → CTA③ → Q&A（6問）→ CTA④ → 会社概要＋メンバー →
  フッター → モバイル固定CTA
- Q&Aの「収益構造グラフ」は画像ではなく **CSSで作図**（棒グラフ・利益タグ）
- 比較表は `<table>` で実装（横幅が足りない端末は表だけ横スクロール）
- 色は `:root` のトークンに集約（`--orange: #cc461d` / `--cream: #f9f6ef` / `--gold: #c2a560` など）
- Google Fonts の Noto Sans JP を使用
- 画像は hero 以外 `loading="lazy"` + `decoding="async"`

### 3-4. 検証で見つけて直した不具合

| 症状 | 原因 | 対応 |
|---|---|---|
| ヘッダーが画面上部に固定されない | `body { overflow-x: hidden }` がスクロールコンテナを作って `position: sticky` を壊していた | `html { overflow-x: clip }` に変更 |
| CTAバンドの見出しが花の装飾に隠れる | 装飾画像が帯の全幅に伸びていた | 装飾を幅 `clamp(90px,15%,200px)` に制限、`.cta__inner` を `z-index: 2` に |
| 花の装飾に見出しやボタンが写り込む | 切り出し範囲が広すぎた | 帯の上下左右を実測し直して再クロップ |
| ヘッダーロゴの下に黒い帯 | クロップ範囲にヒーロー画像が入っていた | 範囲を `(56,14)-(306,72)` に修正 |
| CHAGEEの写真が縦長に潰れる | 埋め込み画像が回転配置されていた | ページから直接クロップ、`aspect-ratio: 2/1` を指定 |
| ヒーローのカップが二重に見える | 背景画像（`hero-bg`）にすでにカップが合成されていた | 前面のカップ画像レイヤーを削除し、背景1枚に統一 |
| 画像差し替えが反映されない懸念 | `_headers` に `immutable` の1年キャッシュを指定していた | `max-age=3600, must-revalidate` に緩和 |

### 3-5. 検証結果

- 全38画像が本番URLで200を返すことを確認
- 横スクロール（overflow-x）なし ── 375px / 960px 両方で確認
- モバイル（375px）でナビ非表示・グリッド1カラム化・下部固定CTA表示を確認
- CTA 6箇所（ヘッダー／バンド4／モバイル固定）がすべて同一URLに解決されることを確認
- 本番URLをブラウザで上から下まで目視確認済み

---

### 3-6. デザイン精度の作り込み（2026-08-15）

デザイン全体像のPNG（`~/Desktop/1786768516-0AMfp6UKGV4DcYgakQms2RuF.png`）と突き合わせ、
初回実装で抜けていた装飾要素を追加した。

| 箇所 | 対応 |
|---|---|
| ISSUEセクションの枠 | オレンジ地＋単純な枠線 → **クリーム地＋二重罫＋下辺V字（シェブロン）**に作り直し。`clip-path` を同形で5層重ねて表現（`--v` = V字の深さ 16.2vw、`--i` = 罫の内側マージン） |
| ISSUEの見出し | 3行を1つの `<h2>` にまとめ、本文より広い版面（1000px）に。本文は610px、市場カードは770pxに分けた |
| 0次募集セクション | 左右に**白い花の装飾**を追加（`orn-fc-left/right.jpg`） |
| 選ばれる4つの理由 | 見出しの背後に**JATTEA透かしロゴ**（`logo-watermark.png` / `brightness(0)` + 13%）。カードに被らないよう見出し下端そろえ |
| Q&A | 同じく見出し背後に透かしロゴ |
| 会社概要 | 左右にトーンオントーンの花柄（`orn-company-left/right.jpg`） |
| フッター | 全面の**花柄パターン**（`orn-footer-left/right.jpg`、左右43%ずつ） |
| メンバーカード | 金の枠を**二重罫**に（外5px＋内1px）、顔写真の縁も4pxに |

追加した素材はすべてデザインPNGからの切り出し。`logo-watermark.png` はロゴのオレンジ地を
輝度でアルファ化して作った白抜きPNG（CSSで暗転させてトーンオントーンにしている）。

検証: 375px / 960px で横スクロールなし、全画像200、透かしロゴが後続要素に被らないことを確認。

---

## 4. 未確定・申し送り事項

### 要対応

1. **LINEのCTAリンクが未設定**
   現在 `https://lin.ee/REPLACE_ME` のまま。URLをもらい次第、
   `public/index.html` 冒頭の `window.JATTEA_LINKS.line` を書き換えて再デプロイする。

   ```js
   window.JATTEA_LINKS = {
     line:   "https://lin.ee/REPLACE_ME",   // ← ここ
     chagee: "https://www.chagee.com/",
     heytea: "https://www.heytea.com/"
   };
   ```

2. **本文リンクの飛び先が仮**
   CHAGEE / HEYTEA は公式サイトのトップを暫定で入れている。意図が違えば変更する。

### PDF側の表記について（そのまま再現している）

- 0次募集セクションの2枚目タイルが、1枚目と同じ「売上からの粗利率」というラベルで
  「23.0%」と表示されている。本文の「ドリンク原価率23%」と照らすと**デザイン側の誤りの可能性**が高いが、
  「PDF通りに忠実」との判断だったのでそのままにしてある。
  直す場合は `index.html` の該当 `<span class="fc__label">` を「ドリンク原価率」に変更。
- ファーストビューの「???万人の圧倒的集客力」もPDFのまま伏せ字。

### そのほか

- 独自ドメインは未設定（`*.pages.dev` のみ）。必要なら Cloudflare Pages 側で追加する。
- `hero-cup.png`（カップ単体の切り抜き画像）は、背景1枚に統一した際に削除済み。
  もし前面レイヤーとして使いたくなったらPDFから再抽出できる。
- ブラウザ自動操作のスクリーンショット機能がこのページで不安定（古い描画を返す）だったため、
  最終確認はDOMの実測値＋本番URLの目視で行った。

---

## 5. 運用コマンド

```bash
cd ~/Desktop/jattea-lp
npm run dev      # ローカル確認 http://localhost:8788
npm run deploy   # Cloudflare Pages へ本番デプロイ
```

初回のみ `npx wrangler login` が必要。
